#include <stdio.h>

int main(void) {
    // char name[100]; // buffer to store the name

    // printf("What's your name? ");
    // scanf("%99s", name); // read up to 99 characters, leaving space for '\0'

    // printf("hello, %s\n", name);
    // return 0;

}
